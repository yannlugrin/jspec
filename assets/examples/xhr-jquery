describe 'Something'
  before_each
    // type and status are optional
    mock_request().and_return('{ foo : "bar" }', 'application/json', 200)
  end
  
  it 'should foo'
    $.getJSON('foo', function(response){
      response.foo.should.eql 'bar'
    })
  end
end